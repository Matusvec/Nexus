# ── Core framework ───────────────────────────────────────
fastapi>=0.109.1
uvicorn[standard]>=0.27.0
starlette>=0.35.0           # directly imported in middleware
pydantic>=2.5.0
pydantic-settings>=2.2.1

# ── Database & ORM ──────────────────────────────────────
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29.0             # async PostgreSQL driver
psycopg2-binary>=2.9.9      # sync driver — used by Alembic migrations
pgvector>=0.2.4
alembic>=1.13.0

# ── LLM & AI ────────────────────────────────────────────
google-generativeai>=0.4.0
numpy>=1.26.0

# ── Clustering / ML ────────────────────────────────────
umap-learn>=0.5.5
hdbscan>=0.8.33
scikit-learn>=1.4.0

# ── Utilities ────────────────────────────────────────────
thefuzz>=0.22.1              # fuzzy string matching
python-dotenv>=1.0.0         # .env loading fallback for pydantic-settings

# ── Provisioned (used in docker-compose, not yet in app code) ──
redis>=5.0.0
